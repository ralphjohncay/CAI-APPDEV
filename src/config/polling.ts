/** How often the product catalog silently refreshes while logged in (ms). */
export const PRODUCTS_POLL_INTERVAL_MS = 1000;

/** How often the Orders screen silently refreshes status while focused (ms). */
export const ORDERS_POLL_INTERVAL_MS = 1000;

/** Product detail screen silent refresh interval (ms). */
export const PRODUCT_DETAIL_POLL_INTERVAL_MS = 1000;
